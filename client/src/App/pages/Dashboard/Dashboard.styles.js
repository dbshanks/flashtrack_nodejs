import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  splashscreen: {
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  logo: {
    width: "35rem",
  },
});
